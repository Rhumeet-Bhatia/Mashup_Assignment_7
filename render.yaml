services:
  - type: web
    name: mashup-service
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: gunicorn app:app